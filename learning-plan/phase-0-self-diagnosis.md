# 阶段 0：自诊断

> **开始时间**：______
> **完成时间**：______

---

## 规则（先读完再开始）

1. **不查任何资料**。包括不打开 dimensions/layers 已有文件、不搜索、不问 Claude。这是 baseline 快照，不是答题。
2. **写不出来就留空**。空白本身是有信息量的——它精确指出了你的盲区位置。不要为了不空着而硬凑词。
3. **写完整句子，不要堆名词**。"我知道 ReAct" 不算骨架。"ReAct 是 reasoning 和 acting 交替的循环模式，每轮模型先输出 thought，再选 action，观察结果后进入下一轮，相比纯 chain-of-thought 多了和外部环境交互的能力" 才算骨架。
4. **顺序不限，可以分多次完成**。但不要跨越太久——拖一周，前面写的就不是 baseline 了。
5. **写完一节立刻自评等级**，不要等全部写完再回头评。
6. **与之后阶段 1 的关系**：这份文档是"before 快照"，永久保留不再修改。阶段 1 你会把骨架的精华迁移并扩充到正式的 dimensions/layers 文件里。

---

## 自评等级定义

- 🟢 **能教别人**：骨架完整，关键概念到位，能讲清主流方案的取舍
- 🟡 **能听懂讨论**：认得名词，能大致复述，但讲不清为什么、什么时候用哪个
- 🔴 **基本空白**：连关键概念都说不全，听人讨论会跟丢

---

## 第一部分：四个工程层级

### L1 Agent 内核

> 提示问题：这一层在解决什么问题？有哪些主流的 agent 循环模式？context 工程有哪些关键决策点？

**凭记忆写出**：

这一层聚焦在Agent本体的运行上，Agent循环模式：
- 从最开始的ReAct，思考 - 行动 - 观察 的循环模式
- 然后由于模型内化了Reasoning，就变成了思考 - 行动的循环，即思考清楚再行动
- 还有，Plan and Excute 模式，但是这种模式在长链路任务上很容易出现误差累加

prompt工程在于思考如何写出更优质的提示词，而context工程在于如何在Agent运行过程中，在合适的时机给到合适的内容。
context的存在，原因在于超长的提示词会分散大模型的注意力，且冗余的内容会给大模型的决策带来污染。

一些关键的处理方式有：
1. 设计锚点进行摘要，以压缩历史上下文
2. 上下文中选择一些合适的信息写出。例如，工具响应的结果或者历史的对话理论上也可以，但是这种写出需要可逆，即我可以在需要的时候检索回来
- 这里关于写出的原则，我设置的是超过6轮以上的工具会进行写出
- 但不是所有工具结果都会进行写出，我选择数据型的工具进行写出
- 一些重要决策类工具都会保留结果
3. 相对独立的场景封装成subagent，单独去执行，避免污染主agent的上下文
4. 还有本身skill的设计，就是所谓渐进式披露，也是一种context工程
5. 还有RAG的应用，外挂知识库也是context工程的一部分。
- RAG的发展也经历了：naive 向量召回，误差率较高已经弃用了
- advanced rag : 输入的扩写后，embedding召回 + 重排序
- agentic rag ：由大模型agent 进行信息的召回


**自评**：⬜ 🟢 / ⬜ 🟡 / ⬜ 🔴 
红色

**最近一次让我意外的失败/困惑**（具体场景）：


目前项目中有一个数据查询工具，返回的数据很大，约1000到数万不等的token占用，因此为了解决这个问题，我让服务端提供了查询接口，并且将这部分查询结果从上下文中进行了折叠，告诉agent在用户下面的对话需要这部分数据时进行检索。
另外还采取了其他的一些压缩手段，例如我有一个需要压缩的工具清单，这部分工具的结果我是写入了mongodb中，这部分数据是仅属于当前session的，也是可以在需要时进行检索。

困惑或者失败还没有遇到，或者想不到，因为项目目前还没有正式推广。

---

### L2 与世界的接口

> 提示问题：tool use 的关键设计决策有哪些？MCP 解决了什么问题？RAG 的主流架构和常见失败模式？memory 系统的几种主流方案？

**凭记忆写出**：

_（在这里写）_

**自评**：⬜ 🟢 / ⬜ 🟡 / ⬜ 🔴

**最近一次让我意外的失败/困惑**：

_（在这里写）_

---

### L3 多 Agent 协作

> 提示问题：多 agent 协作的主流编排模式有哪些？哪些场景值得做 multi-agent，哪些不值得？通信和状态共享怎么处理？

**凭记忆写出**：

_（在这里写）_

**自评**：⬜ 🟢 / ⬜ 🟡 / ⬜ 🔴

**最近一次让我意外的失败/困惑**：

_（在这里写）_

---

### L4 生产化

> 提示问题：Eval 怎么设计才能反映真实失败？tracing 的关键信息是什么？成本怎么控制？安全/越界怎么防？

**凭记忆写出**：

_（在这里写）_

**自评**：⬜ 🟢 / ⬜ 🟡 / ⬜ 🔴

**最近一次让我意外的失败/困惑**：

_（在这里写）_

---

## 第二部分：七个地基维度

> 这部分聚焦"这个能力对 agent 工程意味着什么"，不是 LLM 内部的技术原理（那是 llm-learning 项目的事）。
> 提示问题：这个能力当前的水位？最近 1-2 年发生了什么变化？变化对四层工程产生了什么影响（哪些塌缩、哪些涌现）？

### 维度 1：Reasoning

**凭记忆写出**：

_（在这里写）_

**自评**：⬜ 🟢 / ⬜ 🟡 / ⬜ 🔴

---

### 维度 2：Long Context

**凭记忆写出**：

_（在这里写）_

**自评**：⬜ 🟢 / ⬜ 🟡 / ⬜ 🔴

---

### 维度 3：Multimodality

**凭记忆写出**：

_（在这里写）_

**自评**：⬜ 🟢 / ⬜ 🟡 / ⬜ 🔴

---

### 维度 4：Tool Use

**凭记忆写出**：

_（在这里写）_

**自评**：⬜ 🟢 / ⬜ 🟡 / ⬜ 🔴

---

### 维度 5：Instruction Following

**凭记忆写出**：

_（在这里写）_

**自评**：⬜ 🟢 / ⬜ 🟡 / ⬜ 🔴

---

### 维度 6：Memory

**凭记忆写出**：

_（在这里写）_

**自评**：⬜ 🟢 / ⬜ 🟡 / ⬜ 🔴

---

### 维度 7：Inference-Time Compute

**凭记忆写出**：

_（在这里写）_

**自评**：⬜ 🟢 / ⬜ 🟡 / ⬜ 🔴

---

## 第三部分：诊断汇总（写完上面 11 节后再填）

### 等级分布

把每个项填入对应栏，一眼看出全貌。

| 等级 | Layers | Dimensions |
|------|--------|------------|
| 🟢 能教别人 | _（列出）_ | _（列出）_ |
| 🟡 能听懂讨论 | _（列出）_ | _（列出）_ |
| 🔴 基本空白 | _（列出）_ | _（列出）_ |

### 阶段 1 优先级排序

按"对你工作影响 × 当前薄弱程度"综合排序，得到阶段 1 的填充顺序。优先处理🔴+高影响项，其次🟡+高影响。

**Layers 处理顺序**：
1. _（最优先）_
2. _
3. _
4. _

**Dimensions 处理顺序**（先选 3 个进入阶段 1，其余推迟）：
1. _（最优先）_
2. _
3. _
4. _（推迟）_
5. _（推迟）_
6. _（推迟）_
7. _（推迟）_

### 我自己感到意外的发现

_（写下做完这次自诊断后让你意外的事——某个本以为掌握的领域其实写不出来？某个一直觉得自己不懂的其实写出来不少？这些"意外"通常比等级评分更有诊断价值。）_

### 给阶段 1 的备注

_（任何在阶段 1 推进时要记住的事：特别想搞清楚的问题、想优先读的资源、想避开的坑、需要协调 llm-learning 项目的部分等。）_

---

## 完成后的下一步

1. 在 `learning-plan/README.md` 的进度跟踪里勾掉"阶段 0：自诊断完成"
2. 把阶段 1 优先级清单告诉我，我们根据它细化阶段 1 的资源池和具体安排
3. 这份文档归档保留，**不要再修改**——它是 baseline，未来回看会很有价值
